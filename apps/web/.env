# Non-sensitive defaults. Override per-developer in .env.local (gitignored).
# All values read with parseInt + default in code, so missing keys are fine.

# Gameplay
NEXT_PUBLIC_POLLING_INTERVAL_SECONDS=300
FEED_COOLDOWN_SECONDS=1800
PLAY_COOLDOWN_SECONDS=1200
