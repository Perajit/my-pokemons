{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "db:generate": {},
    "build": {
      "dependsOn": ["^build", "^db:generate"],
      "outputs": [".next/**", "!.next/cache/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "format:check": {}
  }
}
